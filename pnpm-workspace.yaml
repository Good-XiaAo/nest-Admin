packages:
  - 'apps/*'
  - 'packages/*'

# Single source of truth for dependency versions shared across the workspace.
# Reference an entry with "pkg": "catalog:" and bump the version here instead
# of editing every package.json.
catalog:
  typescript: ^5.9.3
