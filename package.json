{
  "name": "nest-admin",
  "version": "1.0.0",
  "private": true,
  "description": "nest-admin pnpm monorepo",
  "scripts": {
    "dev": "pnpm --parallel --filter @nest-admin/web --filter @nest-admin/server dev",
    "dev:web": "pnpm --filter @nest-admin/web dev",
    "dev:server": "pnpm --filter @nest-admin/server dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r --if-present test",
    "test:e2e": "pnpm --filter @nest-admin/server test:e2e",
    "lint": "pnpm -r --if-present lint",
    "type-check": "pnpm -r --if-present type-check",
    "format": "pnpm -r --if-present format"
  },
  "engines": {
    "node": "^22.18.0 || >=24.12.0"
  },
  "packageManager": "pnpm@10.28.2"
}
